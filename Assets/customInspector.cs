using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(p2p))]
public class customInspector : Editor
{
    public override void OnInspectorGUI()
    {
        DrawDefaultInspector();
        
        p2p p = (p2p)target;
        if (GUILayout.Button("Create data channel"))
        {
            p.CreateData();
        }
        if (GUILayout.Button("Send msg"))
        {
            p.SendData();
        }
    }
}
*/